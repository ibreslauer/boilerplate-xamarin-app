<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Boilerplate.App.Templates.MenuItemTemplate"
             Visual="Material">
    <ContentView.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout>
                <Label Padding="15, 15"
                       VerticalOptions="Center" FontSize="Subtitle" Text="{Binding MenuText}"/>
            </StackLayout>            
            <BoxView IsVisible="{Binding HasSeparator}" HeightRequest="1" Color="{StaticResource AppGray}" />
        </StackLayout>
    </ContentView.Content>
</ContentView>